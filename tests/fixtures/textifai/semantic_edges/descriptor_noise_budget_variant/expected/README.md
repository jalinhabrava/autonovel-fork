# Expected Artifacts — descriptor_noise_budget_variant

Expected manuales para variante anti-overfitting.

La variante no duplica source completo. Congela replay input manual con nombres/surfaces nuevos para validar que la policy generaliza sin hardcodes.

Comportamiento esperado:

- `el trazador del umbral` -> enrich sobre `Iven Ral`.
- `la voz del peaje` -> attach role/title sobre `Iven Ral`.
- `el amparo de Sora` -> enrich relacional sobre `Iven Ral`.
- `el forastero`, `el centinela`, `ella`, `vendedor ausente` no generan señal fuerte.
- equivalentes de rol no quedan todos `medium`.
