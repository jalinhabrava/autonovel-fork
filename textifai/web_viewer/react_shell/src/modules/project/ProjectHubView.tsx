import React from 'react';
import { AlertTriangle, CheckCircle2, Search } from 'lucide-react';
import { Button, Metric, TopBar } from '../../common/ui';
import { t } from '../../i18n/ui';

export function ProjectHubView({ projectRows, reducedProjectWarning, chaptersProcessed, artifactsCount, warningsVisible }: { projectRows: React.ReactNode; reducedProjectWarning: boolean; chaptersProcessed: number; artifactsCount: number; warningsVisible: number }) {
  return <section><TopBar title={t('nav.hub')} subtitle="Abrir o reanudar proyecto TextifAI completo. El runtime real 20ch se prefiere si existe." actions={<><Button>New ingestion</Button><Button variant="secondary">Open .txtfai / manifest</Button></>} /><div className="p-5 grid grid-cols-12 gap-5"><div className="col-span-12 lg:col-span-8 space-y-4"><div className="rounded-3xl border border-neutral-200 p-5 bg-neutral-50"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-lg">Workspaces disponibles</h2><p className="text-sm text-neutral-500">Datos reales desde /api/projects. Se evita seleccionar fixture 2ch si hay base 20ch.</p></div><Search size={18} className="text-neutral-500" /></div><div className="mt-4 space-y-3">{projectRows}</div>{reducedProjectWarning ? <div className="mt-4 rounded-2xl border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800"><AlertTriangle size={16} className="inline" /> Proyecto reducido detectado: no usar como base UX final si runtime 20ch existe.</div> : null}</div></div><aside className="col-span-12 lg:col-span-4 space-y-4"><Metric label="Contrato" value="manifest.json" note="Un archivo abre el bundle completo." /><Metric label="Futuro" value=".txtfai" note="Formato empaquetado de workspace." /><div className="rounded-3xl border border-neutral-200 p-5"><h3 className="font-semibold">Salud del proyecto</h3><div className="mt-4 space-y-3 text-sm"><div className="flex items-center gap-2"><CheckCircle2 size={16} /> {`Capítulos indexados: ${chaptersProcessed}`}</div><div className="flex items-center gap-2"><CheckCircle2 size={16} /> {`VaERL/artifacts: ${artifactsCount}`}</div><div className="flex items-center gap-2"><AlertTriangle size={16} /> {`Warnings visibles: ${warningsVisible}`}</div></div></div></aside></div></section>;
}
