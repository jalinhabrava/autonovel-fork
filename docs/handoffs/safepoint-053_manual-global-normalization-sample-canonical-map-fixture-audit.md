# Manual Global Normalization Sample + Canonical Map Fixture Audit

## Product Reading

- `safepoint-052` confirmed hardened chapter extraction is objectively better than the empty-map baseline.
- The main remaining extraction-contract gap is that `CANONICAL_ENTITY_MAP` still reaches chapter extraction empty.
- This phase stores a manual ChatGPT response for real post-`SP-051` `bootstrap_global_normalization` and uses real TextifAI code to build a non-empty `canonical_entity_map` fixture.
- This phase does not recapture chapter extraction with the populated map yet.
- This phase does not call provider/API, write back, merge, promote, mutate canon, or implement chunking.

## Scope

- Add manual global normalization sample for Japanese real-novel batch `ch_001`-`ch_003`.
- Generate canonical map fixture with `build_canonical_entity_map(global_payload)`.
- Add provider-free tests proving sample shape, coverage, map generation, and review safety.
- Add checklist and gap report for future populated-map recapture.
- Update provider samples README.

## Files Changed

- `tests/fixtures/textifai/real_novel/real_bootstrap_prompt_capture/provider_samples/chatgpt_response_bootstrap_global_normalization_ja_batch_001_after_sp051.json`
- `tests/fixtures/textifai/real_novel/real_bootstrap_prompt_capture/expected/canonical_entity_map_from_manual_global_ja_batch_001_after_sp051.json`
- `tests/fixtures/textifai/real_novel/real_bootstrap_prompt_capture/expected/global_normalization_manual_sample_checklist.json`
- `tests/fixtures/textifai/real_novel/real_bootstrap_prompt_capture/expected/global_normalization_to_canonical_map_gap_report.json`
- `tests/test_textifai_real_bootstrap_manual_global_normalization_audit.py`
- `tests/fixtures/textifai/real_novel/real_bootstrap_prompt_capture/provider_samples/README.md`
- `docs/handoffs/safepoint-053_manual-global-normalization-sample-canonical-map-fixture-audit.md`

## Global Normalization Sample

- Sample id: `chatgpt_response_bootstrap_global_normalization_ja_batch_001_after_sp051`.
- Source: manual ChatGPT response to real captured post-`SP-051` `bootstrap_global_normalization` prompt.
- Source language: `ja`.
- Batch scope: `ch_001`, `ch_002`, `ch_003`.
- Stored as provider sample JSON without prompt text or source chapter text.
- Not provider runtime output.
- Not canon approval.

## Canonical Map Generated

- Generated by calling real TextifAI `build_canonical_entity_map(global_payload)`.
- Output fixture: `canonical_entity_map_from_manual_global_ja_batch_001_after_sp051.json`.
- Map contains 17 entries.
- Shape preserved from runtime function.
- Core targets present: `アデルマン・レオフリック`, `ティセイア王国`, `杖の一族`, `ベル`, `均衡`.

## Schema Validation

- Global sample is JSON parseable.
- Top-level includes `work`, `entities`, `merge_plan`.
- `work.title == "王者の杖"`.
- `work.language == "ja"`.
- `entities` is non-empty.
- Relevant entities include global normalization fields required by this audit.

## Useful Coverage

- Sample covers `セラ`, `アデルマン・レオフリック`, `ティセイア王国`, `杖の一族`, `王と杖`, `王者の杖`, `均衡`, `ベル`, `舌のないベル`, `赤子`, `触媒`, `ティセイア王国の崩壊`.
- Coverage spans characters, places, factions, concepts, objects, and events.

## Canonical Map Quality

- Map is non-empty and large enough for populated-map recapture.
- Core persistent entities map to canonical names and aliases.
- `ベル` remains an object target via `entity_kind: object` in source sample and canonical map entry with durable aliases.
- `key_facts` are truncated by runtime builder as expected.

## Review / No Auto-canon Safety

- Uncertain identities remain review/needs_review where appropriate: `赤子`, `証人`, `王と杖`, `ネリス女王`, `仮面の男`.
- `赤子` keeps descriptor/review behavior despite alias overlap with `王者の杖`.
- `証人` includes `私` as alias but stays review.
- `merge_plan` entries include reason and confidence.
- No write-back or canon mutation performed.

## What Worked

- Manual global sample is usable for building a populated canonical map fixture.
- Real `build_canonical_entity_map` produces a stable, non-empty output from the sample.
- Core objects/concepts/entities needed for recapturing chapter extraction are present.
- Review safety remains visible in generated map.

## What Stayed Weak

- Manual sample is not provider runtime output.
- Batch is limited to `ch_001`-`ch_003` and may overfit the opening.
- This phase does not prove chapter extraction behavior with populated map.
- This phase does not audit chunking/reduction path.

## Provider-free Guarantee

- No provider/API calls.
- No network needed for validation.
- No secrets used.
- Tests read fixtures and call local pure code only.

## Tests Added / Updated

- Added `tests/test_textifai_real_bootstrap_manual_global_normalization_audit.py`.
- Updated provider samples README.
- Added checklist and gap report fixtures.

## Validation Performed

- `uv run python -m unittest -v tests.test_textifai_real_bootstrap_manual_global_normalization_audit`
- `uv run python -m unittest -v tests.test_textifai_real_bootstrap_hardened_chatgpt_response_audit`
- `uv run python -m unittest -v tests.test_textifai_bootstrap_prompt_schema_hardening`
- `uv run python -m unittest -v tests.test_textifai_real_bootstrap_prompt_capture_harness`
- `uv run python -m unittest -v tests.test_textifai_structured_bootstrap_v1.StructuredBootstrapV1Tests.test_run_structured_bootstrap_v1_writes_json_artifacts`
- `git status --short`
- `git diff --stat`

## Data Written

- Manual global normalization JSON sample.
- Generated canonical entity map fixture.
- Checklist JSON.
- Gap report JSON.
- Provider-free audit test.
- Handoff.

## Safety Constraints

- No runtime semantic code changed.
- No capture harness changed.
- No provider code changed.
- No chunking/viewer code changed.
- No real `runs/**` or `vault/**` writes.
- No full captured prompt or source novel text committed.

## Known Limitations

- Manual ChatGPT sample may be cleaner than provider runtime output.
- Batch scope is only first three chapters.
- Populated-map chapter extraction has not been recaptured.
- Downstream behavior with this map remains untested.

## Future Extensions

- Add dev-only `--global-normalization-json` to capture harness.
- Recapture real chapter extraction prompt with populated `CANONICAL_ENTITY_MAP`.
- Ask ChatGPT for chapter response against populated-map prompt.
- Compare populated-map response against `SP-052` empty-map hardened response.
- Audit multi-chapter and multilingual behavior before chunking.

## Runtime Changes

- None.

## Provider Calls

- No.

## Write-back

- No.

## Branch

- `phase-1.3-ingestion-vaerl-hardening`

## Next Suggested Phase

- `Phase 1.3.M-b5c-2` — dev-only global-normalization override, recapture chapter prompt with populated canonical map, then compare against `SP-052`.
